syntax = "proto3";

package automation.v1;

message ScrapeContestRequest {
	string url = 1;
}

message ScrapeContestResponse {
	message Problem {
		string url = 1;
		repeated string inputs = 2;
	}
	
	map<string, Problem> problems = 1;
}