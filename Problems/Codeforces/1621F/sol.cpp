#include <bits/stdc++.h>

using namespace std;

int main() {
  cin.tie(0)->sync_with_stdio(0);

  int T;
  cin >> T;

  while (T--) {
    int N, A, B, C;
    cin >> N >> A >> B >> C;

    string S;
    cin >> S;

    // it's just a casework problem
  }
}
