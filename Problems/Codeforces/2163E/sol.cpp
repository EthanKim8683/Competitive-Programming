#include <bits/stdc++.h>

using namespace std;

int main() {
  cin.tie(0)->sync_with_stdio(0);

  string S;
  cin >> S;

  if (S == "first") {
    int T;
    cin >> T;

    while (T--) {
      int N, C;
      cin >> N >> C;

      vector<string> G(N);
      for (auto &e : G) cin >> e;
    }
  } else {
    int T;
    cin >> T;

    while (T--) {
      int N;
      cin >> N;

      string G1;
      cin >> G1;

      string G2;
      cin >> G2;
    }
  }
}
