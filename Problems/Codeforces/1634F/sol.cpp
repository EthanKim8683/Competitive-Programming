#include <bits/stdc++.h>

using namespace std;

using i64 = long long;

int main() {
  cin.tie(0)->sync_with_stdio(0);

  int N, Q, MOD;
  cin >> N >> Q >> MOD;

  vector<int> A(N);
  for (auto &e : A) cin >> e;

  vector<int> B(N);
  for (auto &e : B) cin >> e;
}
