#include <bits/stdc++.h>

using namespace std;

const int MAXC = 200000;

int main() {
  cin.tie(0)->sync_with_stdio(0);

  int R, S, N;
  cin >> R >> S >> N;

  vector<int> A(R);
  for (auto &e : A) cin >> e;

  vector<int> B(S);
  for (auto &e : B) cin >> e;

  vector<int> C(N);
  for (auto &e : C) cin >> e;

  //
}