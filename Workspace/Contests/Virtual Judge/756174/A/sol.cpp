// #include <bits/stdc++.h>
// using namespace std;

// #define int long long
// #define float long double
// #define rep(i, a, b) for (int i = a; i < (b); ++i)
// #define all(x) begin(x), end(x)
// #define sz(x) (int) (x).size()
// #define eb emplace_back
// #define pb push_back
// #define vc vector
// #define fs first
// #define sd second
// typedef pair<int, int> pii;
// typedef vector<int> vi;

// const int MAXP = 1e6;

// signed main() {
//   cin.tie(0)->sync_with_stdio(0);
//   cin.exceptions(cin.failbit);

//   int N, W;
//   cin >> N >> W;

//   // beat at most P/2 total?

//   vc<set<int>> groups(MAXP + 1);
//   rep(i, 0, N) { groups[1].insert(i); }
//   vi ans(N, W * 1);
//   rep(i, 0, W) {
//     int K;
//     cin >> K;
// 		rep(j, 0, K) {
// 			int C;
// 			cin >> C;
// 			C--;
// 			ranks[]
// 		}
//   }
// }