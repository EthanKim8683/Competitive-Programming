#include <bits/stdc++.h>

using namespace std;

int main() {
  cin.tie(0)->sync_with_stdio(0);

  int N, K;
  cin >> N >> K;

  for (int i = 0; i < N; i++) {
    int a, b;
    string expr;
    int r;
    cin >> a >> b >> expr >> r;
  }

  int R;
  cin >> R;
}