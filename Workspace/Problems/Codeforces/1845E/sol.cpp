#include <bits/stdc++.h>

using namespace std;

int main() {
  cin.tie(0)->sync_with_stdio(0);

  int N, K;
  cin >> N >> K;

  vector<int> A(N);
  for (auto &e : A) cin >> e;

  // the range of a ball is pretty constrained, I claim around O(\sqrt(N))
}
