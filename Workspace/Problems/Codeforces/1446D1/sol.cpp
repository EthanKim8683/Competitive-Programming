#include <bits/stdc++.h>

using namespace std;

int main() {
  cin.tie(0)->sync_with_stdio(0);

  int N;
  cin >> N;

  vector<int> A(N);
  for (auto &e : A) cin >> e, e--;

  // where is the earliest point in which it's the maximum?

  for (int i = 0; i < 100; i++) {
  }
}
