#include <bits/stdc++.h>

using namespace std;

using i64 = long long;

const int MOD = 1e9 + 7;

int main() {
  cin.tie(0)->sync_with_stdio(0);

  int N, Q;
  cin >> N >> Q;

  vector<int> A(N);
  for (auto &e : A) cin >> e;
}
