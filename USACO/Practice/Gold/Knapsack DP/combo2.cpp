#include <iostream>
#include <cstdio>

#define MOD 1000000007
 
using namespace std;

using I = int;

I coins[100];
 
int main(void) {
#ifdef ETHANKIM8683
  freopen("combo2.in", "r", stdin);
#endif
  cin.tie(0)->sync_with_stdio(0);
  I n, x;
  cin >> n >> x;
  for (I i = 0; i < x; i++) {
    for (I j = 0; j < n; j++) {
      
    }
  }
  return 0;
}