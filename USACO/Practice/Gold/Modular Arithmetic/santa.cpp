#include <iostream>
#include <cstdio>

using namespace std;

using I = int;

const I N = 1e6;

I main(void) {
#ifdef ETHANKIM8683
  freopen("santa.in", "r", stdin);
#endif
  cin.tie(0)->sync_with_stdio(0);
  I n;
  cin >> n;
  return 0;
}