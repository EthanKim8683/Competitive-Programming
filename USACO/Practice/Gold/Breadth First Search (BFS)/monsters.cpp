#include <iostream>
#include <cstdio>

using namespace std;

using I = int;

const I N = 1000;
const I M = 1000;

I main(void) {
#ifdef ETHANKIM8683
  freopen("monsters.in", "r", stdin);
#endif
  cin.tie(0)->sync_with_stdio(0);
  I n, m;
  cin >> n >> m;
  return 0;
}