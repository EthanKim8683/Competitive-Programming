#include <iostream>
#include <cstdio>

using namespace std;

using I = int;

I main(void) {
#ifdef ETHANKIM8683
  freopen("tracks.in", "r", stdin);
#endif
  cin.tie(0)->sync_with_stdio(0);
  I h, w;
  cin >> h >> w;
  
  return 0;
}