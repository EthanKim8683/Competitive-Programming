#include<bits/stdc++.h>
using namespace std;
using I=int;
using S=string;
const I N=18;
I prms[N][N];
I main(){
  cin.tie(0)->sync_with_stdio(0);
  I n;cin>>n;
  for(I i=0;i<n;i++)for(I j=0;j<n;j++)cin>>prms[i][j];
  I q;cin>>q;
  while(q--){
    S s;cin>>s;
  }
}