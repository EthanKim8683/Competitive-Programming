#include <iostream>
#include <cstdio>
#include <vector>

using namespace std;

using I = int;

vector<I> adjs[100000];

I main(void) {
#ifdef ETHANKIM8683
  freopen("cereal2.in", "r", stdin);
#endif
  cin.tie(0)->sync_with_stdio(0);
  return 0;
}